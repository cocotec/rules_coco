import unqualified Runnable

@runtime(.MultiThreaded)
component Comp {
  val client : Provided<Runnable>
  machine M {
    state Stopped {
      client.start() = setNextState(Started)
    }

    state Started {
      client.stop() = setNextState(Stopped)
    }
  }
}
