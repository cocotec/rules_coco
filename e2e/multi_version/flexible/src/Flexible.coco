// Example Coco source for multi-version testing (flexible package)
// Unpinned; depends on "modern", pinned to 1.5.0, which is also the default. No warning.

port FlexibleInterface {
  function execute() : Nil
  machine {
    execute() = {}
  }
}

@runtime(.SingleThreaded)
external component FlexibleComponent {
  val client : Provided<FlexibleInterface>
}
